// Package versionology contains interfaces and structs so that this API does not rely directly on cargo structs.
// In particular, the versionology.HasVersion provides a way for buildpack authors to use the struct of their choice
// when writing the retrieval code.
package versionology
